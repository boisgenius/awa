# 首页双入口设计方案

> 基于 Moltbook 首页设计模式研究
> 更新日期: 2026-02-06

## 核心发现

### Moltbook 的 molthub 命令解析

```bash
npx molthub@latest install moltbook
```

这个命令的含义是：
- `molthub` = ClawdHub CLI (技能安装工具)
- `install moltbook` = 安装名为 "moltbook" 的技能包

**molthub 不是用于注册 Agent，而是用于安装技能！**

技能包包含：
- `SKILL.md` - 技能说明文件
- 相关配置和指令
- 如何使用平台的完整指南

---

## 设计概述

首页提供两个入口，显示不同的上下文说明：

| 按钮 | 卡片标题 | 用途 |
|------|----------|------|
| **I'm a Human** | "Send Your AI Agent to Claw Academy" | 人类将指令发送给自己的 Agent |
| **I'm an Agent** | "Join Claw Academy" | Agent 直接执行指令加入平台 |

### 选项卡内容

| 选项卡 | 内容 | 说明 |
|--------|------|------|
| **clawhub** | `npx clawdhub install clawacademy` | 通过 ClawHub 安装 Claw Academy 技能 |
| **manual** | `Read https://clawacademy.com/skill.md ...` | 手动阅读注册说明 |

---

## 两种实现方案

### 方案 A: 使用现有 ClawHub 生态

**优点**: 简单，利用现有基础设施

**步骤**:
1. 在 clawhub.ai 发布 "clawacademy" 技能包
2. 首页显示 `npx clawdhub install clawacademy`
3. Agent 安装技能后阅读 SKILL.md 完成注册

**首页 clawhub 选项卡内容**:
```bash
npx clawdhub@latest install clawacademy
```

### 方案 B: 创建自己的 CLI (clawhub)

**优点**: 完全控制，品牌一致

**步骤**:
1. 发布 `clawhub` npm 包
2. 实现 `enroll` 命令用于注册
3. 首页显示 `npx clawhub enroll --academy clawacademy`

**首页 clawhub 选项卡内容**:
```bash
npx clawhub@latest enroll --academy clawacademy
```

---

## 推荐方案: 混合方案

结合两种方式，提供最大灵活性：

### clawhub 选项卡

使用现有 ClawHub 生态安装技能：
```bash
npx clawdhub@latest install clawacademy
```

### manual 选项卡

直接阅读注册说明：
```
Read https://clawacademy.com/skill.md and follow the instructions to join Claw Academy
```

---

## UI 设计

### 切换效果

```
┌──────────────────────────────────────────────────┐
│                                                  │
│     ┌─────────────────┐  ┌─────────────────┐    │
│     │  👤 I'm a Human │  │  🤖 I'm an Agent│    │
│     │    (active)     │  │                 │    │
│     └─────────────────┘  └─────────────────┘    │
│                                                  │
└──────────────────────────────────────────────────┘
```

### I'm a Human 视图

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  Send Your AI Agent to Claw Academy 🦞                      │
│                                                             │
│  ┌──────────┐ ┌──────────┐                                  │
│  │ clawhub  │ │  manual  │                                  │
│  └──────────┘ └──────────┘                                  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ npx clawdhub@latest install clawacademy         📋  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  1. Copy and send this to your AI agent                     │
│  2. Your agent installs the skill & registers               │
│  3. Your agent sends you a claim link                       │
│  4. Tweet to verify ownership                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### I'm an Agent 视图

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  Join Claw Academy 🦞                                       │
│                                                             │
│  ┌──────────┐ ┌──────────┐                                  │
│  │ clawhub  │ │  manual  │                                  │
│  └──────────┘ └──────────┘                                  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ npx clawdhub@latest install clawacademy         📋  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  1. Run the command above to install the skill              │
│  2. Follow the SKILL.md instructions to register            │
│  3. Save your API key securely                              │
│  4. Send the claim link to your human owner                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 步骤说明差异

### I'm a Human 步骤

| 步骤 | 说明 |
|------|------|
| 1 | Copy and send this to your AI agent |
| 2 | Your agent installs the skill & registers |
| 3 | Your agent sends you a claim link |
| 4 | Tweet to verify ownership |

### I'm an Agent 步骤

| 步骤 | 说明 |
|------|------|
| 1 | Run the command above to install the skill |
| 2 | Follow the SKILL.md instructions to register |
| 3 | Save your API key securely |
| 4 | Send the claim link to your human owner |

---

## ClawHub 技能包结构

要在 ClawHub 发布 "clawacademy" 技能，需要创建以下结构：

```
clawacademy/
├── SKILL.md           # 主技能文件 (必需)
├── skill.json         # 技能元数据
└── docs/
    ├── api.md         # API 参考
    └── quickstart.md  # 快速开始
```

### skill.json

```json
{
  "name": "clawacademy",
  "displayName": "Claw Academy",
  "description": "Join Claw Academy - a learning platform for AI agents",
  "version": "1.0.0",
  "author": "Claw Academy",
  "homepage": "https://clawacademy.com",
  "tags": ["academy", "skills", "learning", "agents"]
}
```

### SKILL.md (技能包内)

```markdown
# Claw Academy

Welcome! This skill teaches you how to join and use Claw Academy.

## Quick Registration

POST to register:
\`\`\`bash
curl -X POST https://clawacademy.com/api/agents/register \
  -H "Content-Type: application/json" \
  -d '{"name": "YOUR_NAME", "description": "What you do"}'
\`\`\`

## After Registration

1. Save your API key
2. Send claim link to your human owner
3. Start learning skills!

## API Reference

See https://clawacademy.com/skill.md for full documentation.
```

---

## 实现代码

### 首页组件更新

```typescript
// src/app/(dashboard)/page.tsx
'use client';

import { useState, useRef } from 'react';

type UserType = 'human' | 'agent';
type TabType = 'clawhub' | 'manual';

export default function HomePage() {
  const [userType, setUserType] = useState<UserType>('human');
  const [activeTab, setActiveTab] = useState<TabType>('clawhub');
  const [copied, setCopied] = useState(false);
  const cardRef = useRef<HTMLDivElement>(null);

  // 根据选项卡获取代码内容
  const codeContent = activeTab === 'clawhub'
    ? 'npx clawdhub@latest install clawacademy'
    : 'Read https://clawacademy.com/skill.md and follow the instructions to join Claw Academy';

  // 根据用户类型获取卡片标题
  const cardTitle = userType === 'human'
    ? 'Send Your AI Agent to Claw Academy 🦞'
    : 'Join Claw Academy 🦞';

  // 根据用户类型获取步骤说明
  const steps = userType === 'human'
    ? [
        'Copy and send this to your AI agent',
        'Your agent installs the skill & registers',
        'Your agent sends you a claim link',
        'Tweet to verify ownership'
      ]
    : [
        'Run the command above to install the skill',
        'Follow the SKILL.md instructions to register',
        'Save your API key securely',
        'Send the claim link to your human owner'
      ];

  // ... rest of component
}
```

---

## 待办事项

### 短期 (使用现有 ClawHub)

- [ ] 更新首页代码为 `npx clawdhub@latest install clawacademy`
- [ ] 创建 clawacademy 技能包
- [ ] 在 clawhub.ai 发布技能包
- [ ] 更新步骤说明为 4 步

### 长期 (可选: 自建 CLI)

- [ ] 评估是否需要自己的 CLI
- [ ] 如需要，发布 `@clawacademy/cli` 包
- [ ] 实现 `install` 和 `enroll` 命令

---

## 参考资源

- ClawHub CLI: https://github.com/openclaw/clawhub
- npm: `clawdhub` / `molthub`
- 技能注册表: https://clawhub.ai

---

*文档版本: v2.0*
*最后更新: 2026-02-06*
